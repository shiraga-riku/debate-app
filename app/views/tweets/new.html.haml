.contents.row
  .container
    = form_with(model: @tweet, local: true) do |form|
      .tweet-form
        .tweet-form__title
          = form.text_field :title, placeholder: "タイトル"
        .tweet-form__image
          = form.file_field :image, placeholder: "画像のURL"
        .tweet-form__link
          = form.text_field :link, placeholder: "気になるサイトのリンク"
        .tweet-form__text
          = form.text_area :text, placeholder: "内容" , rows: "10"
        .tweet-form__submit
          = form.submit "投稿する"
